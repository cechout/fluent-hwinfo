<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="FluentHwInfo.Views.CPUPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:FluentHwInfo.Views"
    xmlns:local1="using:FluentHwInfo.ViewModels"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border
            Grid.Row="0"
            VerticalAlignment="Stretch"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="12,0,12,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock
                        Grid.Column="1"
                        Style="{StaticResource Font_TextBlockOutputDisplay}"
                        Text="Sensor" />
                    <TextBlock
                        Grid.Column="2"
                        HorizontalAlignment="Right"
                        Style="{StaticResource Font_TextBlockOutputDisplay}"
                        Text="Aktuell" />
                    <TextBlock
                        Grid.Column="3"
                        HorizontalAlignment="Right"
                        Style="{StaticResource Font_TextBlockOutputDisplay}"
                        Text="Minimum" />
                    <TextBlock
                        Grid.Column="4"
                        HorizontalAlignment="Right"
                        Style="{StaticResource Font_TextBlockOutputDisplay}"
                        Text="Maximum" />
                    <TextBlock
                        Grid.Column="5"
                        HorizontalAlignment="Right"
                        Style="{StaticResource Font_TextBlockOutputDisplay}"
                        Text="Durchschnitt" />
                </Grid>

                <Border
                    Grid.Row="1"
                    Height="1"
                    Margin="0,0,0,8"
                    Background="{ThemeResource DividerStrokeColorDefaultBrush}" />

                <!--  List View  -->
                <ListView
                    Grid.Row="2"
                    HorizontalContentAlignment="Stretch"
                    ItemsSource="{x:Bind ViewModel.SensorList, Mode=OneWay}"
                    SelectionMode="Multiple">

                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="local1:SensorRowViewModel">
                            <Grid Padding="12,0,12,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" MinWidth="80" />
                                    <ColumnDefinition Width="Auto" MinWidth="80" />
                                    <ColumnDefinition Width="Auto" MinWidth="80" />
                                    <ColumnDefinition Width="Auto" MinWidth="80" />
                                </Grid.ColumnDefinitions>

                                <TextBlock
                                    Grid.Column="0"
                                    VerticalAlignment="Center"
                                    Style="{StaticResource BodyTextBlockStyle}"
                                    Text="{x:Bind Name, Mode=OneWay}" />

                                <TextBlock
                                    Grid.Column="1"
                                    Margin="10,0,0,0"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Style="{StaticResource BodyTextBlockStyle}"
                                    Text="{x:Bind CurrentValue, Mode=OneWay}" />

                                <TextBlock
                                    Grid.Column="2"
                                    Margin="10,0,0,0"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Style="{StaticResource BodyTextBlockStyle}"
                                    Text="{x:Bind MinimumValue, Mode=OneWay}" />

                                <TextBlock
                                    Grid.Column="3"
                                    Margin="10,0,0,0"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Style="{StaticResource BodyTextBlockStyle}"
                                    Text="{x:Bind MaximumValue, Mode=OneWay}" />

                                <TextBlock
                                    Grid.Column="4"
                                    Margin="10,0,0,0"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Style="{StaticResource BodyTextBlockStyle}"
                                    Text="{x:Bind AverageValue, Mode=OneWay}" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>


            </Grid>
        </Border>

        <Button
            Grid.Row="1"
            Margin="0,20,0,0"
            HorizontalAlignment="Right"
            Content="Pin to Widget"
            Style="{StaticResource AccentButtonStyle}">
            <Button.ContentTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon FontSize="16" Glyph="&#xE93A;" />
                        <TextBlock VerticalAlignment="Center" Text="{Binding}" />
                    </StackPanel>
                </DataTemplate>
            </Button.ContentTemplate>
        </Button>
    </Grid>
</Page>