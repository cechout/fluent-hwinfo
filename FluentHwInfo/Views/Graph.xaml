<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="FluentHwInfo.Views.Graph"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:FluentHwInfo.Views"
    xmlns:lvc="using:LiveChartsCore.SkiaSharpView.WinUI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:FluentHwInfo.ViewModels"
    mc:Ignorable="d">

    <Border
        Padding="0,0,0,0"
        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
        BorderBrush="{ThemeResource ControlStrokeColorSecondaryBrush}"
        BorderThickness="1"
        CornerRadius="4">

        <Grid CornerRadius="3.3">
            <lvc:CartesianChart
                Height="80"
                Margin="-3,0,-3,-2"
                HorizontalAlignment="Stretch"
                AnimationsSpeed="00:00:00"
                Background="Transparent"
                CornerRadius="20"
                DrawMargin="{Binding ChartMargin, Mode=OneWay}"
                Series="{Binding Series, Mode=OneWay}"
                TooltipPosition="Auto"
                XAxes="{Binding XAxes, Mode=OneWay}"
                YAxes="{Binding YAxes, Mode=OneWay}" />
        </Grid>
    </Border>
</UserControl>
