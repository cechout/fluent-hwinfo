<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="FluentHwInfo.Views.SensorCardControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:FluentHwInfo.Views"
    xmlns:local1="using:FluentHwInfo.ViewModels"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ToggleButton
        MinHeight="0"
        Padding="16,2,16,2"
        HorizontalAlignment="Stretch"
        HorizontalContentAlignment="Stretch"
        BorderThickness="0"
        CornerRadius="0"
        IsChecked="{x:Bind ViewModel.IsSelected, Mode=TwoWay}">

        <ToggleButton.Resources>

            <StaticResource x:Key="ToggleButtonForegroundChecked" ResourceKey="TextFillColorPrimaryBrush" />
            <StaticResource x:Key="ToggleButtonForegroundCheckedPointerOver" ResourceKey="TextFillColorPrimaryBrush" />
            <StaticResource x:Key="ToggleButtonForegroundCheckedPressed" ResourceKey="TextFillColorSecondaryBrush" />

            <StaticResource x:Key="ToggleButtonBackground" ResourceKey="ControlFillColorDisabledBrush" />
            <StaticResource x:Key="ToggleButtonBackgroundPointerOver" ResourceKey="ControlFillColorSecondaryBrush" />
            <StaticResource x:Key="ToggleButtonBackgroundPressed" ResourceKey="ControlFillColorTertiaryBrush" />

            <StaticResource x:Key="ToggleButtonBackgroundChecked" ResourceKey="ControlFillColorSecondaryBrush" />
            <StaticResource x:Key="ToggleButtonBackgroundCheckedPointerOver" ResourceKey="ControlFillColorTertiaryBrush" />
            <StaticResource x:Key="ToggleButtonBackgroundCheckedPressed" ResourceKey="ControlFillColorDefaultBrush" />

            <StaticResource x:Key="ToggleButtonBorderBrush" ResourceKey="CardStrokeColorDefaultBrush" />
            <StaticResource x:Key="ToggleButtonBorderBrushPointerOver" ResourceKey="CardStrokeColorDefaultBrush" />
            <StaticResource x:Key="ToggleButtonBorderBrushPressed" ResourceKey="CardStrokeColorDefaultBrush" />
            <StaticResource x:Key="ToggleButtonBorderBrushChecked" ResourceKey="CardStrokeColorDefaultBrush" />
            <StaticResource x:Key="ToggleButtonBorderBrushCheckedPointerOver" ResourceKey="CardStrokeColorDefaultBrush" />
            <StaticResource x:Key="ToggleButtonBorderBrushCheckedPressed" ResourceKey="CardStrokeColorDefaultBrush" />

        </ToggleButton.Resources>

        <Grid HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" MinWidth="140" />
                <ColumnDefinition Width="*" MinWidth="65" />
                <ColumnDefinition Width="*" MinWidth="65" />
                <ColumnDefinition Width="*" MinWidth="65" />
                <ColumnDefinition Width="*" MinWidth="65" />
            </Grid.ColumnDefinitions>

            <StackPanel
                Grid.Column="0"
                VerticalAlignment="Center"
                Orientation="Horizontal"
                Spacing="8">

                <CheckBox
                    MinWidth="0"
                    IsChecked="{x:Bind ViewModel.IsSelected, Mode=OneWay}"
                    IsHitTestVisible="False" />

                <TextBlock
                    VerticalAlignment="Center"
                    Style="{ThemeResource CaptionTextBlockStyle}"
                    Text="{x:Bind ViewModel.Name, Mode=OneWay}" />
            </StackPanel>

            <TextBlock
                Grid.Column="1"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                FontWeight="Normal"
                Style="{ThemeResource CaptionTextBlockStyle}"
                Text="{x:Bind ViewModel.CurrentValue, Mode=OneWay}" />
            <TextBlock
                Grid.Column="2"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                FontWeight="Normal"
                Style="{ThemeResource CaptionTextBlockStyle}"
                Text="{x:Bind ViewModel.MinimumValue, Mode=OneWay}" />
            <TextBlock
                Grid.Column="3"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                FontWeight="Normal"
                Style="{ThemeResource CaptionTextBlockStyle}"
                Text="{x:Bind ViewModel.MaximumValue, Mode=OneWay}" />
            <TextBlock
                Grid.Column="4"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                FontWeight="Normal"
                Style="{ThemeResource CaptionTextBlockStyle}"
                Text="{x:Bind ViewModel.AverageValue, Mode=OneWay}" />
        </Grid>

    </ToggleButton>
</UserControl>
